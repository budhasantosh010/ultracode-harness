---
name: export
description: Export the current session as JSON
---

Export the current session:

1. Run `opencode export` to export the current session as JSON
2. The export includes conversation history, settings, and state
3. Exported sessions can be imported later with `opencode import <file>`
