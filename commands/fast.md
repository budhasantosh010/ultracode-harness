---
name: fast
description: Toggle fast mode for quicker responses with reduced reasoning
---

Toggle fast mode for quicker responses. Fast mode uses a faster model variant with reduced reasoning effort for simpler tasks. Use `/fast on` to enable, `/fast off` to disable.
