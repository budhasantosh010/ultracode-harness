---
name: plan
description: Toggle plan mode on/off for research-only activity
---

Toggle plan mode:

1. Use `toggle_plan_mode` to switch between plan mode (read-only) and normal mode
2. In plan mode: only read, glob, grep are allowed — no edits or bash
3. Use `/plan on` to enter plan mode, `/plan off` to exit
