---
name: cd
description: Navigate to a different directory
---

Change the working directory. Use bash or shell_exec to change to the specified directory.

Usage: `/cd <path>` — Navigate to the specified path. Supports absolute and relative paths.
